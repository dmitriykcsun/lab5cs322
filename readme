This C program calculates the page number and offset for a given virtual address in decimal. The calculation is done based on a 32-bit address with a 4-KB page size. 

To call the program use "./a address" where address is the virtual address you would like to know the page # and offset of.
